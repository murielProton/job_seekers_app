{% extends 'base.html.twig' %}

{% block title %}JobInterview{% endblock %}

{% block body %}
    <h1>JobInterview</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_interview.id }}</td>
            </tr>
            <tr>
                <th>DateOfInterview</th>
                <td>{{ job_interview.dateOfInterview ? job_interview.dateOfInterview|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Schedule</th>
                <td>{{ job_interview.schedule ? job_interview.schedule|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ job_interview.Address }}</td>
            </tr>
            <tr>
                <th>ContactForName</th>
                <td>{{ job_interview.contactForName }}</td>
            </tr>
            <tr>
                <th>ContactSirName</th>
                <td>{{ job_interview.contactSirName }}</td>
            </tr>
            <tr>
                <th>ContactTitle</th>
                <td>{{ job_interview.contactTitle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_interview_index') }}">back to list</a>

    <a href="{{ path('job_interview_edit', {'id': job_interview.id}) }}">edit</a>

    {{ include('job_interview/_delete_form.html.twig') }}
{% endblock %}

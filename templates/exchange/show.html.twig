{% extends 'base.html.twig' %}

{% block title %}Echange{% endblock %}

{% block body %}
    <h1>Echange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exchange.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ exchange.date ? exchange.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Schedule</th>
                <td>{{ exchange.schedule ? exchange.schedule|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Means</th>
                <td>{{ exchange.means }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ exchange.comments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exchange_index') }}">Retour à la liste</a>

    <a href="{{ path('exchange_edit', {'id': exchange.id}) }}">Modifier</a>

    {{ include('exchange/_delete_form.html.twig') }}
{% endblock %}

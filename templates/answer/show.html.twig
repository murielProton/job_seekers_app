{% extends 'base.html.twig' %}

{% block title %}Answer{% endblock %}

{% block body %}
    <h1>Answer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ answer.date ? answer.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TextOfAnswer</th>
                <td>{% for text in answer.textOfAnswer %}{{ text }}{% endfor %}</td>
            </tr>
        </tbody>
    </table>
    <div>
    <a href="{{ path('answer_index') }}"  class="btn btn-light" role="button">Liste des réponses</a>
    </div><div>
    <a href="{{ path('answer_edit', {'id': answer.id}) }}"  class="btn btn-light" role="button">Modifier</a>
    </div>
    {{ include('answer/_delete_form.html.twig') }}
{% endblock %}
